printf_as_one()
{
    printf "%s" "$*"
}

escape()
{
     printf_as_one ${*//'"'/'\"'}
}

quote()
{
     printf_as_one '"'$(escape $*)'"'
}

debug_log()
{
    [[ $debug ]] && printf "[debug]\t%s\n" "$*"
}
